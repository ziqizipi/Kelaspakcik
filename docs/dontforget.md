Ask claude to generate prompt for the page to implement in design

Put the all page design to codebase

tell claude to see the new front end and tell to remove every checked task to match current front end

tell claude to reapply them checklist to the current front end codebase

Simply tell claude to wire everyhting back to the new frontend
